<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background12"
    android:gravity="bottom">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="20dp"
    android:gravity="center"
    android:orientation="horizontal">
    <Button
        android:id="@+id/startSaoLeiActivityButton"
        android:backgroundTint="#AA98"
        android:layout_width="200dp"
        android:layout_height="150dp"
        android:onClick="startSaoLeiActivity"
        android:textColor="#D31507"
        android:textSize="50dp"
        android:text="扫雷" />

    <Button
        android:id="@+id/start2048ActivityButton"
        android:layout_below="@+id/startSaoLeiActivityButton"
        android:backgroundTint="#AA98"
        android:layout_width="200dp"
        android:layout_height="150dp"
        android:textColor="#0C19EA"
        android:textSize="50dp"
        android:text="2048"
        android:onClick="start2048Activity" />
</LinearLayout>
</RelativeLayout>
